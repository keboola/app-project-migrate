version: '2'
services:
  dev:
    build: .
    volumes:
      - ./:/code
      - ./data:/data
    environment:
      - KBC_URL
      - KBC_TOKEN
      - KBC_TOKEN_NOT_MASTER
      - SOURCE_KBC_URL
      - SOURCE_KBC_TOKEN
